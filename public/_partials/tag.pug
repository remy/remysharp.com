- posts = collections.tags[tag]
.post
  h1.title Tagged with "#{ tag }"
  .post-content
    ul
      for post in posts
        li
          a(href=`${ post.url }`) #{ post.data.title }
          small.date &nbsp; #{ moment(post.data.date).format('d-MMM yyyy')}
